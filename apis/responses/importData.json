{
  "type": "object",
  "properties": {
    "time": {
      "type": "string",
      "description": "Estimated time to complete import"
    },
    "importId": {
      "type": "string",
      "description": "Unique ID to get import progress details"
    },
    "logFile": {
      "type": "string",
      "description": "Generated import log file name if there are errors or warnings"
    }
  },
  "required": [
    "time",
    "importId"
  ]
}